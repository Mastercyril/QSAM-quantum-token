# Next.js UI Layer

This repo currently has no existing Next.js app.

If UI orchestration is needed, create it under this directory and wire it to `qai2_unified/orchestration/api.py` using the same runtime contract and release-gate metrics.
